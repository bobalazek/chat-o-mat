<ul id="sidebar-list" class="nav nav-pills nav-stacked">
	<li class="{{ path('members-area') == app.currentUri ? 'active' : '' }}"
		role="presentation">
		<a href="{{ url('members-area') }}">
			<i class="fa fa-fw fa-dashboard"></i>
			{{ 'Dashboard'|trans }}
		</a>
	</li>
	<li class="{{ path('members-area.chat') in app.currentUri ? 'active' : '' }}" role="presentation">
		<a href="{{ url('members-area.chat') }}">
			<i class="fa fa-fw fa-comment"></i>
			{{ 'Chat'|trans }}
		</a>
	</li>
	{% if is_granted('ROLE_ADMIN') %}
		<li class="{{ path('members-area.users') in app.currentUri ? 'active' : '' }}" role="presentation">
			<a href="{{ url('members-area.users') }}">
				<i class="fa fa-fw fa-users"></i>
				{{ 'Users'|trans }}
			</a>
		</li>
	{% endif %}
	{% if is_granted('ROLE_ADMIN') %}
		<li class="{{ path('members-area.integrations') in app.currentUri ? 'active' : '' }}" role="presentation">
			<a href="{{ url('members-area.integrations') }}">
				<i class="fa fa-fw fa-wrench"></i>
				{{ 'Integrations'|trans }}
			</a>
		</li>
	{% endif %}
	{% if is_granted('ROLE_ADMIN') %}
		<li class="{{ path('members-area.emojis') in app.currentUri ? 'active' : '' }}" role="presentation">
			<a href="{{ url('members-area.emojis') }}">
				<i class="fa fa-fw fa-smile-o"></i>
				{{ 'Emojis'|trans }}
			</a>
		</li>
	{% endif %}
	{% if is_granted('ROLE_ADMIN') %}
		<li class="{{ path('members-area.statistics') in app.currentUri ? 'active' : '' }}" role="presentation">
			<a href="{{ url('members-area.statistics') }}">
				<i class="fa fa-fw fa-bar-chart"></i>
				{{ 'Statistics'|trans }}
			</a>
		</li>
	{% endif %}
	{% if is_granted('ROLE_ADMIN') %}
		<li class="{{ path('members-area.settings') in app.currentUri ? 'active' : '' }}" role="presentation">
			<a href="{{ url('members-area.settings') }}">
				<i class="fa fa-fw fa-cog"></i>
				{{ 'Settings'|trans }}
			</a>
		</li>
	{% endif %}
</ul>
